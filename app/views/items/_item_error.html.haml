-if @item.errors.any?
  #error_explanation
    %ul
      -@item.errors.full_messages_for(content).each do |message|
        %li
          -if content == :name && @item.name.length > 40
            40文字以内で入力してください 
          -elsif content == :name
            入力してください
          -elsif content == :detail
            入力してください
          -elsif content == :image
            画像を選択してください
          -elsif content == :price
            300以上9999999以下で入力してください
          -elsif content == :size && params[:category_id].nil?
          -else
            選択してください